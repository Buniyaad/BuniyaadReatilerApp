import * as React from 'react';
import {StyleSheet, Image} from 'react-native';
import {
  Container,
  Form,
  Content,
  Text,
  Item,
  Input,
  Label,
  Button,
} from 'native-base';

export default class NotVerified extends React.Component{
    render(){return(
        <Text>Unfortunatley You cannot access this page until admin verifies your account</Text>
    )}
    
}